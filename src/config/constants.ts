export const MOVE_SPEED = 120;
export const SMALL_JUMP_FORCE = 360;
export const BIG_JUMP_FORCE = 450;
export const BIG_MARIO_DURATION_SECONDS = 6;

export enum Tags {
  COIN = 'coin',
  MUSHROOM = 'mushroom',
  DEATH = 'death',
}

export enum Scenes {
  GAME = 'game',
  LOSE = 'lose',
}
